// Author: 
#include <stdio.h>
#include "quiz1.h"

int main(void) {
  z = readline("Enter an integer: ");
  bas3 = readline("Enter a base: ");
  f = quiz1(z, int bas3);
  printf("answer is %d.",f);
}
